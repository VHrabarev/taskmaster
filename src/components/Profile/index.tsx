import { Box, Typography } from '@mui/material';

const Profile = function() {
    return (
        <Box component="section">
            <Typography component="h2" variant='h4'>Profile</Typography>
        </Box>
    );
};

export default Profile;